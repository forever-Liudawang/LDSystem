import React from 'react'
export default function Form(props: { children: JSX.Element }) {
    const {children} = props
    if(!Array.isArray(children)){
        
    }
    console.log(children,'children==>>>')
    return (
        <div>
            
        </div>
    )
}
